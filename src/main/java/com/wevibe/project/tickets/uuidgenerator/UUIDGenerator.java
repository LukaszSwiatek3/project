package com.wevibe.project.tickets.uuidgenerator;

import java.util.UUID;

public class UUIDGenerator {
    public UUID generator() {
        UUID uuid = UUID.randomUUID();
        return uuid;
    }
}
